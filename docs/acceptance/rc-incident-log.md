# RC Incident Log（观察期记录，不进发版判定）

> 记录规则：偶发问题也记录，哪怕最终确认非 Bug。
> 目标不是"测试通过"，而是"使用 N 天 / M 台设备 / 断网 X 次 / 重启 Y 次，无数据异常"。

## 使用基线

| 项 | 值 |
|---|---|
| 冻结版本 | v1.1-rc1（`51a80b8`，tag 不可移动） |
| 服务端 | VPS `http://203.0.113.10:28080`（TTL 2h 正式值） |
| 设备 | Windows PC（双实例：`C:\ln-a` / `C:\ln-b`） |
| 观察开始 | 2026-08-15 |

## 事件记录

| 日期 | 现象 | 可复现 | 数据影响 | 严重级别 | 处理 |
|---|---|---|---|---|---|
| （示例）08-15 | 同步后 UI 未即时刷新 | 否（切换后正常） | 无 | — | 已知 GUI-003a 前行为，rc1 已修复，观察 |

## 阶段性结论（无事件时按周期填写）

```text
使用天数：
设备数：
断网次数：
重启次数（客户端/服务端）：
数据异常：无
```

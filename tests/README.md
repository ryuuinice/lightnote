# LightNote Tests

**Agent 5 (QA / Integration)** 负责此目录。

```text
tests/
├── integration/    # 双客户端 + 真实 Server
├── sync/           # 同步矩阵
├── crash/          # 崩溃恢复
├── blob/           # Blob 传输
└── e2e/            # 端到端
```
